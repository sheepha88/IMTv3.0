import React from 'react';
import ReactDOM from 'react-dom/client';
import QCStatusDialog from './QCStatusDialog';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <QCStatusDialog />
  </React.StrictMode>
); 